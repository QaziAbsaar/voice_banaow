fastapi>=0.111.0
uvicorn>=0.29.0
python-multipart>=0.0.9
demucs>=4.0.1
librosa>=0.10.2
soundfile>=0.12.1
pydub>=0.25.1
ffmpeg-python>=0.2.0
numpy>=1.26.4
aiofiles>=23.2.1
gtts>=2.5.0
python-dotenv>=1.0.1
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0
google-api-python-client>=2.130.0

# RVC inference (required for Convert + TTS pages)
# NOTE: rvc-python may need numpy<=1.23.5 — if conflict arises, run:
#   pip install "numpy<=1.23.5" && pip install rvc-python
rvc-python>=0.1.5

# TTS (XTTS) optional — pip install TTS for voice cloning quality
# praat-parselmouth — needed for pitch extraction during training (Colab only, not backend)
